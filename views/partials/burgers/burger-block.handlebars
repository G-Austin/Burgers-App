<li>
	{{burger_name}}

	<button class="change-devourable" data-id="{{id}}" data-newdevourable="{{devourable}}">
		{{#if devourable}}EAT ME!{{/if}}
	</button>

	<!-- <button class="devour-burger" data-id="{{this.id}}">DEVOUR!</button> -->
</li>
